function start_clock(){

    //new : 내장 객체를 instance화 한다.
    var today =new Date()  // 날짜 객체 생성.
    // console.log(today)
    // console.log(today.toLocaleString())
    // setInterval 함수 : 일정한 시간 후에 작업을 한번 실행합니다.
    setInterval(function(){},1000)
    var my_div = document.getElementById("myDiv",my_div.innerText = today.today)
}